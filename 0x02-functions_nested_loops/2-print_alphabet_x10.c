#include <main.h>

/**
 * main - print 10 time the alphabet
 *
 * Return: 0
 */
void print_alphabet_x10(void);
{

	char ch;
	int i;

	while (i < 10)
	{
		ch = 'a'
		while (ch <= 'z')
		{
			_puchar(ch);
			ch++
		}
		_putchar('\n');
		i++
		return (0);
	}
}
